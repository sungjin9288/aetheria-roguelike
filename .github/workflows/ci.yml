name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

env:
  NODE_VERSION: '20'

jobs:
  static-checks:
    name: Lint + Type-check + Unit tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install art pipeline Python dependencies
        # tests/art-asset-contract.test.js, tests/equipment-art-pipeline.test.js,
        # tests/equipment-signature-art*.test.js shell out to Pillow-based scripts
        # (scripts/*.py) — without this the unit job fails ~105 tests before any
        # assertion about the game itself runs.
        run: pip install -r requirements-art.txt

      - name: Lint
        run: npm run lint

      - name: Type check
        run: npm run type-check

      - name: Unit tests
        # 9 byte-exact art reproducibility checks stay opt-in (AETHERIA_ART_REPRO=1) —
        # they depend on the original asset-generation platform, not just Pillow's version.
        # See requirements-art.txt and docs/AUDIT_REFACTOR_DEVELOP_PLAN_2026-09.md §7.
        run: npm run test:unit

      - name: Build
        run: npm run build

      - name: Build guard
        run: npm run build:guard

  e2e:
    name: E2E (Playwright)
    runs-on: ubuntu-latest
    needs: static-checks
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        # playwright.config의 chromium-mobile 프로젝트는 devices['iPhone 12']를 사용 →
        # iPhone 디바이스 기본 엔진은 webkit이므로 webkit도 설치해야 한다 (chromium만 설치 시 launch 실패).
        run: npx playwright install --with-deps chromium webkit

      - name: Run E2E tests
        run: npm run test:e2e
        env:
          # mock 모드: Firebase 익명 인증 스킵 (헤드리스 환경 안정성).
          # cycle 58: ?e2e=1 URL 플래그가 mock 모드 활성.
          E2E_MODE: '1'
          # 더미 Firebase config(시크릿 아님 — 가짜값). firebase.ts가 빌드 시
          # VITE_FIREBASE_CONFIG 부재면 initializeApp({})로 앱이 부팅 못 함.
          # e2e 모드는 실제 firebase 호출을 스킵하므로 가짜 config로 부팅만 시키면 됨.
          VITE_FIREBASE_CONFIG: '{"apiKey":"e2e-key","authDomain":"e2e.firebaseapp.com","projectId":"e2e-proj","appId":"e2e"}'

      - name: Upload Playwright report
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7
