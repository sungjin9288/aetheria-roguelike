# Required-five boss sources — work in progress

## Current runtime adoption

Verification closed: focused40/40; full unit4360/4360, desktop/mobile smoke, E2E59/59+57/57; actual390×844 controlled combat portraits5/5; Android debug and iOS unsigned packages26/26 JS+authored-PNG byte checks. `native-checkpoint.json` pins current package paths/hashes. No device installation or overall S3 completion claim. The following adoption-stage pending sentence is historical; current progress is authoritative above.

`adoption-selection.json` supersedes the earlier `selection-provenance.json` for runtime selection. Frost Lord now uses the `-compact` master/export: complete framing, larger visible silhouette and independent Sol review Critical0/Important0 at32/46px. The other four selected files are unchanged. All previous variants and exact prompts remain preserved; there were12 built-in calls total, no paid API fallback.

Five authored runtime pins have been added through the existing v1 correction registry and catalog generator. Fresh focused tests40/40 PASS (`/tmp/aetheria-required-five-green-0907.log`), preceded by expected RED missing five pins. All other249 catalog records and PNG hashes exactly match `preimage-manifest.json`; catalog identity SHA remains082bd214bbbe344bcec4934d114b5c1a384b6641b2d86d6169fc34aecb3628d7. Full/direct/native verification is pending; no install or overall design completion claim.

## Historical selected-five checkpoint

The text below is historical source preparation, not current runtime state.

Current selection: five candidates are now available in `selection-provenance.json`. Two fresh built-in generations resolved visible clipping/opaque background: primordial god uses compact inward hands; frost lord uses a short complete crown and contained cloak. Frost selection is explicitly `monster-34852baa4124-framed.png`; the earlier unframed master/export remains preserved but is not selected. `framing-prompts.md` records both new calls (nine built-in calls total for this source batch). Owner viewed both originals and160px exports. All five selected exports pass actual alpha/transparency/8px clear border; runtime manifest is still unchanged. Independent32/46px and runtime adoption remain pending.

Raw-source nuance: new primordial master has14 alpha=1 pixels on the top row. At alpha>1 the bounds start at y9; visible head/halo/hand/body are entirely within frame. Frost master has no top-row nonzero pixels. No opacity cleanup or threshold substitution was applied to either export: the existing strict any-alpha8px export rule still passes. Record this as faint source fringe, not clipped visible anatomy and not perfect source cleanup.

## Historical first source-preparation attempt

The remaining text describes the initial seven-call candidate checkpoint; its pending framing/alpha statements are superseded by the current selection above. Original provenance and rejected attempts remain unchanged.

No runtime adoption. The current manifest still matches `preimage-manifest.json` byte-for-byte.

Four candidate masters and160px size-only exports are preserved for 서리 군주, 차원 파쇄자, 차원 포식자 and 타락한 세계수 수호자. Owner viewed their originals and160px exports. Existing pixel inspector confirmed actual alpha, transparent pixels and8px export margin4/4. This does not prove source anatomy is unclipped: Frost Lord's crown reaches the original top edge and needs framing review/correction before acceptance. Small-size32/46px and independent review remain pending.

원시의 신 is not accepted. Its initial original clipped hand/halo tips. A framing edit reconstructed the complete body but painted an opaque checkerboard; a targeted alpha-only edit also produced an opaque checkerboard. Both edits have hasAlpha=false/hasTransparentPixels=false. All three unsuccessful originals are preserved under `rejected/`; do not normalize an opaque rectangle and call its added outer padding genuine cutout transparency. Next use a fresh, compact-composition built-in generation for this boss rather than repeat the failed opaque edit chain. No paid CLI/API fallback is authorized or used.

`prompts.md` records all five original generation prompts and both failed edit prompts (seven built-in calls total). `provenance.json` pins the four master/export/previous sets, three rejected originals, prompt bytes and preimage manifest. Previous runtime PNGs for the four candidate targets remain unchanged. The prior v3 batch is the latest verified runtime/native checkpoint; no full/native rerun is warranted for this source-only batch yet.

Next: resolve primordial-god alpha/framing and frost crown framing; review all five at160/46/32; TDD exact authored pins and any required classifications; adopt as one batch; focused/direct combat/independent/full/evidence/native verification. Do not count this partial source preparation as S3 completion.
