# First9 boss-body candidate preparation

User approved the nine-body design on 2026-09-08. Built-in imagegen produced nine individual masters; all selected masters are preserved unchanged here and their generated paths are in generation-records.json.

## Current adoption checkpoint

**Final local checkpoint:** full72064 exit0, unit4423/4423, E2E117/117, desktop/mobile smoke, type/lint/build PASS. Desktop smoke reported `browser.close timeout` after `ok`; preserve that warning, not an app regression claim. Art/content, equipment combat-power/economy and event-reward verification exited0. Cap/doctor54606, Android26846 and iOS51758 all exited0. Sol/xhigh independent post-adoption audit **C0/I0**, including all9 screenshots and exact source chain/other245 preservation. The non-blocking stale design status was corrected.

Latest unsigned iOS: `/tmp/aetheria-boss20-ios.ANk5DZ/Build/Products/Release-iphoneos/App.app`. Android debug: `android/app/build/outputs/apk/debug/app-debug.apk`, 226132357 bytes, SHA256 `41931a2fcefb39f803082250d3ad6b23231f4d111dd3b96c412ebb874721e014`. Production main `assets/index-B8QCe31m.js`, SHA256 `3731774ab423cbe0231d48641aeb464d97dd6c13aec9c7b89b885e91768dbedc`. Existing `output/verify-v8-native.py` verified328 paths per package; separate dark-greatsword source/dist/APK/iOS equality adds1, total329. This covers254 monsters,53 location files,18 avatars,2 Earth Verdict images, dark greatsword and main JS. This does not claim all native behavior or an installed latest build.

Native tracked drift0, index empty and git diff --check PASS. Historical candidate6 hash `ac1956ad67087ddeae0cd1af85e57eee7a6bf94d3afb81b00a823f2ceb5ea0dd` and Toss38 hash `0f870e56774c46dcbd477ceda6e9c6c1f312a4a7dec3f84ad10b3342934d5b5c` unchanged (sorted repository-relative path + NUL + bytes). Owned `boss20-review` browser closed; natural QA profile and existing server preserved. Doctor notes absent distribution/release signing; these do not block debug/unsigned verification. No physical Android, signing, device install, archive change, commit or publication. First9 slice is locally complete; remaining8 design, boss-source7 disposition, Kingdom access, device/lifecycle and whole-Goal S6 remain open. Intermediate pending statements below are history.

Corrected candidate re-review by Sol/xhigh: **C0/I0**. Both broken-blade and separated-leg findings were resolved before adoption. Adoption tests first failed on the absent mappings, then focused integration passed **21/21** (`/tmp/aetheria-boss20-adoption-focused.log`, session40337 exit0). Production generator rehearsal confirmed exact9 source/hash changes and other245 metadata/PNG bytes unchanged; only those9 runtime PNGs, new authored sources, registry mappings and generated manifest were adopted. Previous originals remain preserved.

Owner opened all9 actual390×844 combat screenshots under `output/playwright/v20-stable-*-390x844.png`. CLI profile `boss20-review` used isolated device-QA storage, not the natural-play profile. Images decoded160×160 and rendered46×46; all9 had overflowfalse and errors[]. These are controlled portrait fixtures, not natural boss encounters or boss stats/pattern/balance evidence. Attack/skill/item/escape controls remain visible below the log.

Canonical diagnostic refresh succeeded (session37782). Compared with previous/progression-diagnostic-v2.json, the only scalar difference is sources/94/sha256 for src/data/monsterArtManifest.json (`c2b3fe48…` → `c88aec89…`). Report, v1 baseline and all other fields are unchanged. Full gate session72064 is running; art/content session45543 exited0. Native packaging and post-adoption independent audit remain pending. Latest completed native is still the story-identity checkpoint. No installation, commit or publication.

The following preparation entries are chronological history, not current adoption status.

## Preparation and tests

Corrected candidates: resentful-champion `e01f2bb2…` now has a short broad broken blade; rift-commander `45d67ab2…` has two separated spectral leg/foot masses. Full hashes are in prepare.py and exports/receipt.json. The owner opened refreshed review2/3 at original detail; exact2 export changes/other7 byte-identical to exports-review1 confirmed. Focused9/9 PASS session66366 (`/tmp/aetheria-boss20-corrected-focused.log`). Selected generated-original/master9 equality and current254 runtime preimage equality PASS. Independent re-review is running. New adoption tests2 are intentionally RED (`/tmp/aetheria-boss20-adoption-red.log`): registry still lacks these bodies and current hashes remain prototypes. They must turn GREEN after reviewed adoption; no runtime change has been made yet.

Independent first review: **C0/I2, batch adoption held**. Resentful champion's long pointed blade did not read as broken at46/32; rift commander's lower body merged into one spectral taper instead of two marching legs. Other7 had no blocking silhouette/contrast finding. First selected masters for those two and initial review sheets are preserved under rejected/ and review-initial/. Only these two are being regenerated to the already-approved body contract; this is not a new design scope. Source safety9/9 does not override the visual blockers.

- Initial focused integration: boss-body2 + endpoint2 + source-preflight5 = **9/9 PASS**, session92620 exit0, `/tmp/aetheria-boss20-focused.log`. Corrected candidates were rechecked9/9 in session66366 before adoption21/21. These initial checks cover candidate preparation and source admission only, not adoption or actual combat UI.

- New tests/boss-body-monster-source.test.js: RED2 due to absent preparer (`/tmp/aetheria-boss20-red.log`). Related first run3/4: nearest-neighbor downsampling dropped the lowest opaque pixel of the judge, yielding bottom151 instead of152 (`/tmp/aetheria-boss20-green.log`).
- The v19 processing pattern is reused. After resize, crop only the remaining alpha bounds before centering/bottom placement. This removes empty resampling margins, does not paint or stretch anatomy. Final4/4 PASS including endpoint regressions (`/tmp/aetheria-boss20-green-final.log`). Tests exercise deterministic two-run bytes, original preservation, binary alpha/160px/margins/aspect ratio, every master SHA drift and existing-output refusal.
- exports-initial preserves the first preparation. exports/receipt.json pins selected masters and final candidate bytes; final export log `/tmp/aetheria-boss20-export-final.log`.
- Owner opened review-1/2/3.png at original detail: all9 at160/46/32 on light/dark. Thin detail becomes sparse at32, particularly dark steel. Distinct shield, broken sword, wide slab arms, divided torso and envoy tails are visible in the contact sheet; independent acceptance is pending, not implied by a SHA check.
- Focused ESLint and git diff --check PASS. Current254 runtime manifest SHA pins verified; no runtime, registry, game numeric, native or baseline evidence changed.

## Candidate corrections and rejected outputs

Outpost/herald initial masters have true alpha and diffuse glow. Existing alpha128 treatment removed the backdrop in reviewed exports while retaining ghost legs and three tails. Separate image edits instead painted checkerboards (outpost confirmed RGB, no alpha); these are preserved in rejected and not selected. The first dimension-splitter had a cyan waist bridge; rejected/dimension-splitter-connected.png preserves it. A new actual RGBA candidate with a broad empty waist replaces only the selected master; its generated original is also preserved. No checkerboard color-removal hack is used.

The three targeted edit outputs are review history only. The initial export is not a final candidate. Selected body nine requires independent Sol/xhigh review, then exact9 adoption TDD, other245 preservation, actual390 UI, deterministic source-pin refresh/full gates/native packaging. Latest completed native remains story-identity, not this batch.
