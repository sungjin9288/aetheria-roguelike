# S3 priority-nine candidates — 2026-09-07

Status: **nine runtime portraits adopted; focused40/40, direct combat, independent review, full and local native packaging PASS**. Whole-game visual acceptance and device installation remain open.

Final checkpoint: `verify:full` process28312exit0, unit4360/4360, desktop/mobile smoke and E2E59/59+57/57. Known non-blocking desktop browser.close timeout remains disclosed. Cap sync7868, Androiddebug57471 and iOSunsigned21643 all exit0. `native-checkpoint.json` records actual APK/App byte verification: main JS and all20 authored portrait PNGs equal dist (21 assets per package and synced platform directory). No device installation, new signing, upload, commit or publication occurred. Art report548 exports SHAe0516536788d96f8b2751b02eb434e08be63a6c0ab0a9063375f0a66149d51b5; Toss web80725539bytes<83886080; equipment/event/mobile doctor PASS, distribution signing inputs absent. Full and build handles below are now terminal and must not be repolled/restarted. Subsequent pending labels are historical intermediate records.

Owner viewed all nine actual390×844 combat screenshots at `output/playwright/completion-20260907/priority-nine-combat-{1..9}-390x844.png`, in provenance-entry order. Exact portrait paths decoded at160px before capture, console errors/warnings0. These use an isolated deviceQa item-investment save, production MONSTERS identity through reducer dispatch, and controlled forest/level2/combat statistics; they do not prove natural encounters or boss balance. QA-only React engine access was used without changing production/test API source. The initial CLI quoting SyntaxError ran no scenario; corrected run succeeded. Task-owned browser/dev server were closed. Required web-game client boot screenshot/state were inspected separately. Sol documentation re-review resolved the one Important; final C0/I0.

Diagnostic evidence write/verify passed with exact report and v1 baseline unchanged; full verification process28312 is still running. See latest progress/ledger for terminal status before starting native packaging. No current-nine installation is claimed.

Owner inspected `output/playwright/completion-20260907/priority-nine-final-160-46-32.png` at160/46/32px on dark/light surfaces. Silhouettes distinguish the intended bodies; faces and fine robe/metal decoration lose detail at32px. The first blank screenshot and subsequent4321 wrong-app404 screenshot are failed harness attempts, not review evidence. File protocol was blocked and4321 occupied; the successful sheet used a task-owned loopback HTTP server on64938. All9 images returned200; favicon404 is unrelated to artwork. Task-owned browser and HTTP server are now closed; no existing server was stopped.

TDD: existing reviewed contracts expanded for20 pinned portraits and exact crystal→construct/Necron→caster classification. RED2 failed for missing9 pins and two humanoid fallbacks; GREEN related40/40 passed, including all254 regenerated byte checks. Production uses existing authored/v1 path/schema; no global classification or gameplay numeric changes. Exact9 manifest entries/runtime PNGs changed;245 entries and PNG hashes are unchanged. New catalog SHA `082bd214bbbe344bcec4934d114b5c1a384b6641b2d86d6169fc34aecb3628d7`. The nine replaced runtime preimage PNGs are preserved under v3/previous; separately, all eleven previously authored correction entries and PNGs are unchanged. These are different preservation sets, not conflicting counts.

Sol xhigh independently confirmed nine bodies/roles at160/46/32px, all27 master/preimage/export hashes, size-only reproduction9/9, runtime equality9/9,245 unchanged entries and old11 correction preservation; independent monster tests10/10 PASS. Review raised one Important documentation issue: stale pre-adoption prose was mixed with current status. The snapshot below is now explicitly historical. Actual combat/full/native checks remain outstanding.

## Historical source-preparation snapshot — before runtime adoption

The following paragraphs describe only the earlier candidate checkpoint. Their statements about an unchanged runtime and unperformed small-size/adoption work are historical, superseded by the current status above. `provenance.json` and `candidate-pixel-check.json` retain the corresponding pre-adoption seals without rewriting history.

Nine separate built-in imagegen calls produced new originals for 심연의 크라켄, 묘지기 네크론, 혈월의 뱀파이어 로드, 기계 장군, 빙결의 마녀, 아이스 드래곤, 마력 결정체, 거대 지렁이 and 고원 그리핀. No CLI image API was used. Exact prompts are in `prompts.md`; `masters/` contains original outputs, `exports/` size-only candidates and `previous/` the unchanged runtime preimages. The existing `scripts/normalize-monster-source.mjs` exported each to 160×160 without painting or recoloring. Original provider outputs remain preserved outside the repository too.

Owner inspected all nine generated originals and all nine 160px exports. They visibly correct the required body/element mismatch: cephalopod, necromancer, upright vampire noble, mechanical commander, cold witch, ice dragon, mineral cluster, limbless segmented worm, eagle/lion griffin. This is not yet 32/46px, independent-review or actual combat-screen acceptance. In particular, the humanoid candidates have fine decorative detail whose small-size readability still needs scrutiny.

`provenance.json` pins nine master/previous/export hashes, exact prompt bytes and the pre-adoption manifest. All previous files still equal their production PNGs; the entire runtime manifest remains byte-identical to the preimage. `candidate-pixel-check.json` records the existing inspector's results: nine 160×160 PNGs, actual alpha/transparency and no painted pixels within the 8px border. These checks establish file integrity and placement, not artistic quality or provider-call proof.

Pre-adoption regression: `node --import tsx --test tests/monster-catalog-art.test.js` passed10/10, log `/tmp/aetheria-priority-nine-pre-adoption-tests-0907.log`, process95784 exit0. This checks the unchanged existing catalog, not adoption of the new candidates. `git diff --check` passed. Full/runtime/native gates were not rerun because this checkpoint only adds preserved source candidates and notes; they remain required after adoption.

Next: inspect all candidates at 32/46px against dark/light backgrounds; fix any readability defects; TDD the exact correction pins and any necessary exact-name classifications; adopt the nine as one coherent batch using the existing authored/v1 registry. Then regeneration/focused integration, 390×844 combat coverage, independent review, full/evidence/native gates. Do not overwrite historical evidence or claim these candidates are in an installed build. Existing body8 full/native results remain the last runtime checkpoint.
